/**
 * Application-wide constants that are safe to keep in source.
 * These are NOT secrets and NOT environment-specific.
 */
export const SERVICE_NAME = 'tema-middleware';
export const DEFAULT_VERSION = '0.1.0';
